#include <task.h>
#include <stdlib.h>

Message msg;

int main()
{

	int i,t;

    Echo("synthetic12 task F started.");
	Echo(itoa(GetTick()));

for(i=0;i<100;i++){
	
		Receive(&msg,taskE_12);
			for(t=0;t<1000;t++){
		}
		Receive(&msg,taskD_12);

	}

	Echo(itoa(GetTick()));
    Echo("synthetic12 task F finished.");

	exit();

}
