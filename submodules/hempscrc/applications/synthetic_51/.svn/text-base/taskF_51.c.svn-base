#include <task.h>
#include <stdlib.h>

Message msg;

int main()
{

	int i,t;

    Echo("synthetic51 task F started.");
	Echo(itoa(GetTick()));

for(i=0;i<100;i++){
	
		Receive(&msg,taskE_51);
			for(t=0;t<1000;t++){
		}
		Receive(&msg,taskD_51);

	}

	Echo(itoa(GetTick()));
    Echo("synthetic51 task F finished.");

	exit();

}
