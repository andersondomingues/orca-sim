#include <task.h>
#include <stdlib.h>

Message msg;

int main()
{
	
	int i, j,t;

    Echo("testA started.");
	Echo(itoa(GetTick()));

	msg.length = 30;
	for(j=0;j<30;j++) msg.msg[j]=i;
	
	Send(&msg,testB);
	Send(&msg,testC);
	Send(&msg,testD);

}

    Echo(itoa(GetTick()));
    Echo("testA finished.");
	exit();
}
