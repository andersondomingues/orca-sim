#include <task.h>
#include <stdlib.h>

Message msg;

int main()
{

	int i,t;

    Echo("synthetic67 task F started.");
	Echo(itoa(GetTick()));

for(i=0;i<100;i++){
	
		Receive(&msg,taskE_67);
			for(t=0;t<1000;t++){
		}
		Receive(&msg,taskD_67);

	}

	Echo(itoa(GetTick()));
    Echo("synthetic67 task F finished.");

	exit();

}
