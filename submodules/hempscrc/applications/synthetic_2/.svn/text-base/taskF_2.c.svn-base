#include <task.h>
#include <stdlib.h>

Message msg;

int main()
{

	int i,t;

    Echo("synthetic2 task F started.");
	Echo(itoa(GetTick()));

for(i=0;i<100;i++){
	
		Receive(&msg,taskE_2);
			for(t=0;t<1000;t++){
		}
		Receive(&msg,taskD_2);

	}

	Echo(itoa(GetTick()));
    Echo("synthetic2 task F finished.");

	exit();

}
