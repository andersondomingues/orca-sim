#include <task.h>
#include <stdlib.h>

Message msg;

int main()
{

	int i,t;

    Echo("synthetic61 task F started.");
	Echo(itoa(GetTick()));

for(i=0;i<100;i++){
	
		Receive(&msg,taskE_61);
			for(t=0;t<1000;t++){
		}
		Receive(&msg,taskD_61);

	}

	Echo(itoa(GetTick()));
    Echo("synthetic61 task F finished.");

	exit();

}
