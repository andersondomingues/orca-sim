#include <task.h>
#include <stdlib.h>

Message msg;

int main()
{

	int i,t;

    Echo("synthetic64 task F started.");
	Echo(itoa(GetTick()));

for(i=0;i<100;i++){
	
		Receive(&msg,taskE_64);
			for(t=0;t<1000;t++){
		}
		Receive(&msg,taskD_64);

	}

	Echo(itoa(GetTick()));
    Echo("synthetic64 task F finished.");

	exit();

}
